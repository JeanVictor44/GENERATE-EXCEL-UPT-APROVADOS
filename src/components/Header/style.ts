import styled from "styled-components";

export const Container = styled.header `
    padding:10px;
    background-color:var(--blue);

    display:flex;   
    align-items:center;
    gap:10px;
    img {
        width:50px;
    }
 
    h1 {
        color:#FFF;
    }
`